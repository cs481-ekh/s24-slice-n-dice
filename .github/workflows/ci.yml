name: CI
on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]
    workflow_dispatch:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Run build.sh 🏗️
              run: ./build.sh
    test:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: Run test.sh 🧪
              run: ./test.sh
